import React from 'react';
import CustomSlick from '../CustomSlick/CustomSlick';
const TopCountryRow = () => {
   
      return (
        <div>
          <CustomSlick/>
        </div>
      );
    };

export default TopCountryRow;
